(function(){
  let currentDesign=null,selectedZoneId=null,dragState=null;
  function ensureDesigns(){if(!Array.isArray(state.config.designs))state.config.designs=[]}
  function designById(id){ensureDesigns();return state.config.designs.find(d=>d.id===id)}
  function escHtml(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
  function initUI(){
    const nav=document.querySelector('.sidebar nav');
    if(nav&&!document.querySelector('[data-view="design"]')){const b=document.createElement('button');b.className='nav-item';b.dataset.view='design';b.textContent='Design';nav.insertBefore(b,nav.querySelector('[data-view="pairing"]'));b.addEventListener('click',()=>{document.querySelectorAll('.nav-item').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.view').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('design').classList.add('active');$('pageTitle').textContent='Design';renderDesignList()})}
    const main=document.querySelector('.main');
    if(main&&!$('design')){const section=document.createElement('section');section.id='design';section.className='view';section.innerHTML=`<article class="panel"><div class="panel-head"><div><h2>Visual Composer</h2><span>Crea layout multi-area da assegnare agli schermi</span></div><div class="card-actions"><button class="btn" id="newDesignBtn">+ Nuovo design</button><button class="btn btn-primary" id="saveDesignBtn">Salva design</button></div></div><div class="design-layout"><div><div class="design-toolbar"><input id="designName" placeholder="Nome template" style="max-width:260px"><button class="mini-btn" id="addZoneBtn">+ Aggiungi area</button><button class="mini-btn danger" id="deleteZoneBtn">Elimina area</button></div><div class="design-canvas-wrap"><div id="designCanvas" class="design-canvas"></div></div><div id="designTemplateList" class="design-template-list"></div></div><aside class="design-properties"><div class="panel"><h2>Proprietà area</h2><div id="zoneProps" class="design-empty">Seleziona o crea un'area.</div></div></aside></div></article>`;main.appendChild(section)}
    const form=$('screenForm');
    if(form&&!$('screenDesign')){const anchor=form.querySelector('label select[name="playlistIds"]')?.parentElement;const label=document.createElement('label');label.innerHTML='Design schermo<select name="designId" id="screenDesign"><option value="">Nessun design · contenuti full-screen</option></select>';if(anchor)anchor.insertAdjacentElement('afterend',label);else form.querySelector('.modal-actions').insertAdjacentElement('beforebegin',label)}
    $('newDesignBtn')?.addEventListener('click',newDesign);
    $('saveDesignBtn')?.addEventListener('click',saveDesign);
    $('addZoneBtn')?.addEventListener('click',addZone);
    $('deleteZoneBtn')?.addEventListener('click',deleteZone);
    $('designName')?.addEventListener('input',()=>{if(currentDesign)currentDesign.name=$('designName').value});
    $('designCanvas')?.addEventListener('pointerdown',canvasPointerDown);
    window.addEventListener('pointermove',canvasPointerMove);
    window.addEventListener('pointerup',()=>dragState=null);
    form?.addEventListener('submit',screenSubmitCapture,true);
    ensureDesigns();renderScreenDesignOptions();renderDesignList();newDesign();
  }
  function screenSubmitCapture(e){
    e.stopImmediatePropagation();e.preventDefault();
    (async()=>{const f=new FormData(e.currentTarget),editId=f.get('editId'),name=f.get('name');const playlistIds=Array.from($('screenPlaylist').selectedOptions).map(o=>o.value);if(!playlistIds.length){notice('Seleziona almeno una playlist.','warn');return}const designId=f.get('designId')||'';if(editId){const i=state.config.screens.findIndex(x=>x.id===editId);if(i<0){notice('Schermo non trovato.','warn');return}const current=state.config.screens[i];state.config.screens[i]={...current,id:editId,name,location:f.get('location')||'',orientation:f.get('orientation')||'landscape',playlistIds,playlistId:playlistIds[0],designId};await saveConfig('Schermo aggiornato')}else{const id=f.get('id')||slug(name);if(state.config.screens.some(s=>s.id===id)){notice('Esiste già uno schermo con questo ID.','warn');return}state.config.screens.push({id,name,location:f.get('location')||'',orientation:f.get('orientation')||'landscape',playlistIds,playlistId:playlistIds[0],designId,status:'offline'});await saveConfig('Schermo creato')}e.currentTarget.reset();$('screenModal').close();renderScreenDesignOptions()})()
  }
  const originalEditScreen=window.editScreen;
  window.editScreen=function(id){originalEditScreen(id);const s=state.config.screens.find(x=>x.id===id);renderScreenDesignOptions();if($('screenDesign'))$('screenDesign').value=s?.designId||''}
  const originalRenderAll=renderAll;
  renderAll=function(){originalRenderAll();ensureDesigns();renderScreenDesignOptions();renderDesignList()}
  function renderScreenDesignOptions(){ensureDesigns();const sel=$('screenDesign');if(!sel)return;const val=sel.value;sel.innerHTML='<option value="">Nessun design · contenuti full-screen</option>'+state.config.designs.map(d=>`<option value="${escHtml(d.id)}">${escHtml(d.name)}</option>`).join('');if([...sel.options].some(o=>o.value===val))sel.value=val}
  function renderDesignList(){ensureDesigns();const box=$('designTemplateList');if(!box)return;box.innerHTML=state.config.designs.length?state.config.designs.map(d=>`<div class="design-template-row"><div><strong>${escHtml(d.name)}</strong><span class="muted">${(d.zones||[]).length} aree</span></div><div class="card-actions"><button class="mini-btn" onclick="window.__editDesign('${d.id}')">Modifica</button><button class="mini-btn danger" onclick="window.__removeDesign('${d.id}')">Elimina</button></div></div>`).join(''):'<div class="empty">Nessun design salvato.</div>'}
  function newDesign(){currentDesign={id:'design-'+Math.random().toString(36).slice(2,7),name:'Nuovo design',zones:[]};selectedZoneId=null;if($('designName'))$('designName').value=currentDesign.name;renderCanvas();renderProps()}
  function saveDesign(){if(!currentDesign)return;currentDesign.name=($('designName').value||'Design').trim();ensureDesigns();const i=state.config.designs.findIndex(d=>d.id===currentDesign.id);const copy=JSON.parse(JSON.stringify(currentDesign));if(i>=0)state.config.designs[i]=copy;else state.config.designs.push(copy);saveConfig('Design salvato').then(()=>{renderDesignList();renderScreenDesignOptions()})}
  function editDesign(id){const d=designById(id);if(!d)return;currentDesign=JSON.parse(JSON.stringify(d));selectedZoneId=currentDesign.zones[0]?.id||null;$('designName').value=currentDesign.name;renderCanvas();renderProps();document.querySelector('[data-view="design"]')?.click()}
  function removeDesign(id){if(state.config.screens.some(s=>s.designId===id)){notice('Design assegnato a uno schermo: rimuovilo prima di eliminarlo.','warn');return}state.config.designs=state.config.designs.filter(d=>d.id!==id);saveConfig('Design eliminato').then(()=>{renderDesignList();renderScreenDesignOptions();if(currentDesign?.id===id)newDesign()})}
  window.__editDesign=editDesign;window.__removeDesign=removeDesign;
  function addZone(){if(!currentDesign)return;const n=currentDesign.zones.length+1;const z={id:'zone-'+Math.random().toString(36).slice(2,7),name:'Area '+n,x:5+(n-1)*3,y:5+(n-1)*3,w:45,h:45,sourceType:'playlist',sourceId:state.config.playlists[0]?.id||''};currentDesign.zones.push(z);selectedZoneId=z.id;renderCanvas();renderProps()}
  function deleteZone(){if(!currentDesign||!selectedZoneId)return;currentDesign.zones=currentDesign.zones.filter(z=>z.id!==selectedZoneId);selectedZoneId=currentDesign.zones[0]?.id||null;renderCanvas();renderProps()}
  function selectedZone(){return currentDesign?.zones.find(z=>z.id===selectedZoneId)}
  function renderCanvas(){const c=$('designCanvas');if(!c)return;c.innerHTML=(currentDesign?.zones||[]).map(z=>`<div class="design-zone ${z.id===selectedZoneId?'selected':''}" data-zone="${z.id}" style="left:${z.x}%;top:${z.y}%;width:${z.w}%;height:${z.h}%"><span class="design-zone-label">${escHtml(z.name)} · ${escHtml(sourceLabel(z))}</span><span class="design-zone-handle" data-resize="1"></span></div>`).join('')}
  function sourceLabel(z){if(z.sourceType==='media')return state.config.media.find(m=>m.id===z.sourceId)?.name||'Media';return state.config.playlists.find(p=>p.id===z.sourceId)?.name||'Playlist'}
  function renderProps(){const box=$('zoneProps'),z=selectedZone();if(!box)return;if(!z){box.className='design-empty';box.innerHTML='Seleziona o crea un\'area.';return}box.className='';box.innerHTML=`<div class="design-properties"><label>Nome area<input id="zoneName" value="${escHtml(z.name)}"></label><div class="grid-2"><label>X %<input id="zoneX" type="number" min="0" max="95" step="0.1" value="${z.x}"></label><label>Y %<input id="zoneY" type="number" min="0" max="95" step="0.1" value="${z.y}"></label><label>Larghezza %<input id="zoneW" type="number" min="5" max="100" step="0.1" value="${z.w}"></label><label>Altezza %<input id="zoneH" type="number" min="5" max="100" step="0.1" value="${z.h}"></label></div><label>Sorgente<select id="zoneSourceType"><option value="playlist" ${z.sourceType==='playlist'?'selected':''}>Playlist</option><option value="media" ${z.sourceType==='media'?'selected':''}>Media singolo</option></select></label><label>Contenuto<select id="zoneSource"></select></label></div>`;['zoneName','zoneX','zoneY','zoneW','zoneH'].forEach(id=>$(id).addEventListener('input',syncProps));$('zoneSourceType').addEventListener('change',()=>{z.sourceType=$('zoneSourceType').value;z.sourceId='';fillSourceSelect();renderCanvas()});$('zoneSource').addEventListener('change',()=>{z.sourceId=$('zoneSource').value;renderCanvas()});fillSourceSelect()}
  function fillSourceSelect(){const z=selectedZone(),sel=$('zoneSource');if(!z||!sel)return;const list=z.sourceType==='media'?state.config.media:state.config.playlists;sel.innerHTML=list.map(x=>`<option value="${escHtml(x.id)}">${escHtml(x.name)}</option>`).join('');if(list.some(x=>x.id===z.sourceId))sel.value=z.sourceId;else{z.sourceId=list[0]?.id||'';sel.value=z.sourceId}}
  function syncProps(){const z=selectedZone();if(!z)return;z.name=$('zoneName').value||'Area';z.x=Math.max(0,Math.min(95,Number($('zoneX').value)||0));z.y=Math.max(0,Math.min(95,Number($('zoneY').value)||0));z.w=Math.max(5,Math.min(100-z.x,Number($('zoneW').value)||5));z.h=Math.max(5,Math.min(100-z.y,Number($('zoneH').value)||5));renderCanvas()}
  function canvasPointerDown(e){const zoneEl=e.target.closest('.design-zone');if(!zoneEl)return;selectedZoneId=zoneEl.dataset.zone;const z=selectedZone(),rect=$('designCanvas').getBoundingClientRect();dragState={mode:e.target.dataset.resize?'resize':'move',startX:e.clientX,startY:e.clientY,rect,x:z.x,y:z.y,w:z.w,h:z.h};renderCanvas();renderProps();e.preventDefault()}
  function canvasPointerMove(e){if(!dragState)return;const z=selectedZone();if(!z)return;const dx=(e.clientX-dragState.startX)/dragState.rect.width*100,dy=(e.clientY-dragState.startY)/dragState.rect.height*100;if(dragState.mode==='move'){z.x=Math.max(0,Math.min(100-z.w,dragState.x+dx));z.y=Math.max(0,Math.min(100-z.h,dragState.y+dy))}else{z.w=Math.max(5,Math.min(100-z.x,dragState.w+dx));z.h=Math.max(5,Math.min(100-z.y,dragState.h+dy))}renderCanvas();renderProps()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(initUI,0));else setTimeout(initUI,0)
})();